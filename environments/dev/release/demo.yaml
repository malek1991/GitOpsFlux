apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: demo
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: demowebchart
      version: 	0.1.3
      sourceRef:
        kind: HelmRepository
        name: webdevdemo
        namespace: flux-system
      interval: 1m
